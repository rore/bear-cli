package com.bear.app;

final class CheckBlockedMarker {
    static final String RELATIVE_PATH = "build/bear/check.blocked.marker";
    static final String REASON_LOCK = "LOCK";
    static final String REASON_BOOTSTRAP = "BOOTSTRAP_IO";

    private CheckBlockedMarker() {
    }
}
