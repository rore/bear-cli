plugins {
    id 'application'
}

dependencies {
    implementation project(':kernel')
}

application {
    mainClass = 'com.bear.app.BearCli'
    applicationName = 'bear'
}

tasks.named('run') {
    workingDir = rootProject.projectDir
}
