[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "boundary-bypass-third-party"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["requests"]

[tool.mypy]
python_version = "3.12"
strict = true
