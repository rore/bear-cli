[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "check-from-os-import"
version = "0.1.0"
requires-python = ">=3.12"

[tool.mypy]
python_version = "3.12"
strict = true
